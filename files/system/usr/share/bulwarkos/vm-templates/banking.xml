<domain type='kvm'>
  <name>banking</name>
  <title>BulwarkOS — Banking AppVM</title>
  <description>Maximum-isolation VM for financial sites only. Minimal footprint — no shared clipboard, no shared folders.</description>

  <!-- Intentionally minimal RAM — banking sites need very little -->
  <memory unit='MiB'>2048</memory>
  <currentMemory unit='MiB'>1024</currentMemory>
  <vcpu placement='static'>2</vcpu>

  <os firmware='efi'>
    <type arch='x86_64' machine='q35'>hvm</type>
  </os>

  <features>
    <acpi/>
    <apic/>
  </features>

  <cpu mode='host-passthrough' check='none' migratable='off'/>

  <clock offset='utc'>
    <timer name='rtc' tickpolicy='catchup'/>
    <timer name='pit' tickpolicy='delay'/>
    <timer name='hpet' present='no'/>
  </clock>

  <on_poweroff>destroy</on_poweroff>
  <on_reboot>restart</on_reboot>
  <on_crash>destroy</on_crash>

  <devices>
    <emulator>/usr/bin/qemu-system-x86_64</emulator>

    <!-- Fedora Cloud Base clone — thin qcow2 backed by downloaded image -->
    <disk type='file' device='disk'>
      <driver name='qemu' type='qcow2' discard='unmap'/>
      <source file='/var/lib/bulwarkos/images/banking.qcow2'/>
      <target dev='vda' bus='virtio'/>
      <boot order='1'/>
    </disk>

    <interface type='network'>
      <source network='bulwark-appvms'/>
      <model type='virtio'/>
    </interface>

    <!-- SPICE with no clipboard sharing — intentional -->
    <graphics type='spice' autoport='yes'>
      <listen type='none'/>
      <image compression='off'/>
      <!-- Clipboard sharing explicitly disabled for maximum isolation -->
      <clipboard copypaste='no'/>
    </graphics>

    <video>
      <model type='virtio' heads='1' primary='yes'>
        <acceleration accel3d='no'/>
      </model>
    </video>

    <!-- No spiceport channel — no clipboard, no file drag-drop -->

    <input type='tablet' bus='usb'/>
    <input type='keyboard' bus='virtio'/>

    <memballoon model='virtio'>
      <stats period='5'/>
    </memballoon>

    <rng model='virtio'>
      <backend model='random'>/dev/urandom</backend>
    </rng>

    <serial type='pty'>
      <target type='isa-serial' port='0'/>
    </serial>
    <console type='pty'>
      <target type='serial' port='0'/>
    </console>
  </devices>
</domain>
